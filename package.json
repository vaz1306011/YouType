{
  "name": "youtype",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c magenta,cyan \"uvicorn backend.main:app --reload\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend",
    "preview": "npm run preview -w frontend"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
